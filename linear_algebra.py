def curse_of_dimensionality(observations_in_dimension, total_in_dimension, total_observations, dimensions):
    density = total_observations / division**dimensions
    if dimensions = 1:
        dataset = list(range(observations))
        p = observation_in_dimension / total_in_dimension
    if dimension > 1:
        dataset =

    return(density)
